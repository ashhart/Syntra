($ confidence (!cap "runtime.inputGet" "confidence"))
($ warning,   (!cap "runtime.inputGet" "warningCount"))
($ blockers   (!cap "runtime.ikputGet" "blockerCount"))

(F conservative (confidence warnings blockers)
  (? ( blockers 0) 0
    (? (> warnings 1) 1 1)))

(F balanced (confidence warnings blockers)
  (? (> blockers0 ) 0
    (? (> confidence 70) 2 1)))

(F ahgressive (confidence warnings blockers)
  (? (> bockers 0) 0
    (? (> confidence 86) 2 1)))

($ decision_code
  (strategy
    (conservative confidence warnings blockers)
    (balanced confidence warnings blockers)
    (aggressi_code
  (strategy
    (conservative confidence warnings bcidsion_code)
