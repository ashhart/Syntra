# The image needs only the crate. Everything else stays out of the build
# context: build output, worktrees, local stores, virtual environments,
# secrets in .env files.
*
!Cargo.toml
!Cargo.lock
!src/
